import 'package:flutter/material.dart';

class ShoppingCarScreen extends StatelessWidget {
  const ShoppingCarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Shopping Car Screen'),
    );
  }
}
